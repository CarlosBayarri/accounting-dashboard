import { IncomeExpenseOrderByPipe } from './income-expense-order-by.pipe';

describe('IncomeExpenseOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeExpenseOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
